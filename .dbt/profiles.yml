default:
  outputs:
    dev:
      type: bigquery
      method: oauth
      project: dbt-globant
      dataset: dbt_fpelaez_globant  # Asegúrate de que coincida con el dataset en BigQuery
      location: us-west1
      threads: 4
  target: dev
